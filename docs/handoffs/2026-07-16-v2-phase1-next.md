# Handoff：v2.0 Architecture Guardian Phase 1

日期：2026-07-16
来源任务：NVIDIA/SkillSpector 对比 review、版本治理收口和 2.0 开发启动

## 已完成并可复核

- 用户所称 “Skill Spectre” 已核实为 `NVIDIA/SkillSpector`；对比基线固定为 commit `8f534e2951e0b7d0b8fb8e84832cd3605f95c032`。
- 已吸收 Python AST 行为检查、局部 source-to-sink 风险关联和 SARIF 2.1.0 输出；默认文本与 `--json` CLI 兼容保持不变。
- 0.1.1 的测试、Ruff、官方 Skill validation、production Doctor、credential lint、diff check 和文档/schema 检查均通过；全量 pytest 为 116 passed。
- 2.0 Phase 1 的 Blueprint schema/model、canonical JSON、SHA-256 fingerprint、unknown/legacy、extensions 敏感值门禁和基础 migration/rollback 回归已完成。
- 变更已提交 `c58d389`，推送到 `codex/version-roadmap`，Draft PR #2：<https://github.com/wukongai/skill-engineering/pull/2>。

## 未完成但已明确归属

以下事项属于当前 `docs/sprints/2026-07-v2.0-architecture-guardian.md`，不是已完成发布，也不应被误报为 Backlog 已交付：

1. 为 atomic/router/orchestrator/adapter 建立最小 Blueprint fixture 与负例。
2. 从现有 1.x Skill/contract 生成可重复的只读 Blueprint inventory，保持不接入 apply。
3. 补齐真实 inventory 的 schema、migration、rollback、敏感信息和 0.1.x 回归 evidence。

下一位执行者应先读取 `docs/TASK.md`、当前 Sprint、2.0 Spec/Plan 和本文件，再实现第 1 项；完成后更新 Task、Sprint、Daily Log 和测试 evidence。

## 明确未执行

- 未创建 tag。
- 未进行公开发布或自动 Global enable。
- 未把任何 apply、迁移或 Guardian 自动修改接入生产路径。
- `uv.lock` 以及工作区中其他未跟踪的 Skill identity unification 草案文件，本次 review 均未纳入提交；后续执行者应先确认其归属，禁止误删或覆盖。

## 保护性约束

- 保持 1.x CLI、JSON 和 contract 兼容；先 preview/read-only，再讨论 apply。
- 候选生成不得接收 holdout assertion 或 baseline score。
- 任何 release、tag、publish 或 Global scope 都需要单独明确授权。
