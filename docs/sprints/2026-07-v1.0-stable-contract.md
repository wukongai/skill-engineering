# Sprint：v1.0 Stable Lifecycle Contract

状态：Release Candidate 许可证收口已通过；runtime/bootstrap 与正式 tag 仍待完成

日期：2026-07-18

## 目标

把已经通过真实用户旅程验证的 0.1.x 生命周期能力，收敛为用户可以稳定安装、创建、检查、维护和回滚的 1.0 契约，并使远程默认分支与《Skill Engineering 1.0 开源与安装指南》一致。

## 进入范围

- 唯一身份 `skill-engineering` 与标准 `npx skills add` 安装入口；
- CLI、JSON schema、审批和错误边界冻结；
- wheel、Python 支持矩阵、干净环境安装；
- 会议纪要、GitHub Issue、Web 验收、研究证据包四个 Use Case；
- README、版本源、Changelog、Roadmap、Task、发布日志和测试证据对齐。
- Apache-2.0、NOTICE、citation、品牌边界和包元数据在首个稳定 tag 前冻结。

## 不进入范围

- Architecture Guardian、Blueprint inventory 和 semantic diff 的 2.0 正式能力；
- PyPI 发布、自动 Global 安装、自动 tag 或未经批准的 GitHub Release；
- 用静态 Doctor 分数宣称跨模型真实效用。

## 任务

- [x] 统一 Agent Skill 名称和远程安装命令。
- [x] 冻结 1.x 公开契约和 0.1.x schema 兼容策略。
- [x] 增加版本事实源自动一致性检查。
- [x] 补充 Python 3.10/3.12/3.14 CI 支持矩阵。
- [x] 在同一个 1.0.0 候选上运行四个 Use Case。
- [x] 完成 wheel、干净环境、pytest、Ruff、Skill validation、Doctor、credential lint 和 diff check。
- [x] 推送默认分支并从远程默认分支重新安装验证。
- [ ] 补齐 Agent Skill-only 干净环境的 runtime/bootstrap，并完成 create → apply → doctor；安装器发现成功不能替代完整运行验收。
- [x] 通过 ADR 0006 将当前候选迁移为 Apache-2.0，并补齐 NOTICE、citation 与品牌边界。
- [x] 完成许可证迁移后的全量门禁、PM 复核与 OB task 对齐。
- [ ] 用户单独批准 `v1.0.0` tag 和 GitHub Release。

## 退出条件

所有门禁绑定同一个候选 commit；远程安装只发现 `skill-engineering`；四个 Use Case 结果和限制被如实记录；文档不把 2.0 Preview 当成 1.0 Stable。
