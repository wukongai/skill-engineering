# 1.1 Review Remediation RED → GREEN 证据

日期：2026-07-30

对应 Spec：[`2026-07-30-v1.1-review-remediation-spec.md`](../specs/2026-07-30-v1.1-review-remediation-spec.md)

## 结论

Codex 交叉评审 CR-01 至 CR-06 已形成回归并修复。结果只证明创建主链路的
结构、安全和确定性行为已满足本 Spec；真实跨宿主效用仍以独立 E2E 为准。

| Review finding | RED | GREEN |
|---|---|---|
| CR-01 普通创建仍跳 legacy scaffold | 根路由包含“新建使用 `skill-engineering create`” | 普通创建固定走 Native Authoring + `native_plan.py`;legacy 仅 1.0/CI `scaffold_only` |
| CR-02 Brief 原文落盘 | 磁盘 JSON 包含测试敏感值 | `save_authoring_brief()` 自动保存脱敏副本且不修改调用对象 |
| CR-03 单 Skill 安装无法评分 | wrapper 缺 CLI 退出 2 | `creation_review.py` 在 `python -I -S` 下输出四维分数、coverage、`utility_claim=false` |
| CR-04 self-test 只编译 | runtime exception、timeout 被误报通过并生成 bytecode | manifest 测试在临时副本真实执行；runtime/timeout/uncovered 阻断，目标不变 |
| CR-05 Content Gate 漏检 | 环境文件、PEM、symlink、越界、目录名、副作用、manifest 漏过 | CG004–CG010 回归全部通过；Skill Engineering 自举 Content Gate 通过 |
| CR-06 `content_status` 可篡改 | v2 plan 改成 `content_complete` 仍可 Apply | hash v2 覆盖完成语义；legacy 无字段保守映射 `scaffold_only` |

## 关键命令与真实结果

- `pytest -q`：211 passed。
- `ruff check .`：All checks passed。
- `skill-engineering lint skills/skill-engineering`：ok。
- `skill-engineering doctor ... --profile production`：0 FAIL / 0 WARN，100/A。
- `skill_self_test.py skills/skill-engineering --json`：Content Gate、portable review、
  4 个声明式 usage test 全部通过，`target_unchanged=true`。
- `credential-lint.sh --all`：通过。
- `check-release.py --version 1.1.0`：通过，1.1 保持 Unreleased。
- `git diff --check`：通过。

## TDD 分组

- Authoring：5 passed。
- BuildPlan/scaffold/maintenance 兼容：22 passed。
- Content Gate：27 passed。
- Portable review、Doctor wrapper、声明式 self-test：相关回归全绿。
- Native Plan：9 passed（含持久化 Brief 未脱敏阻断）。
- Native route/host adapters：21 passed。

## 边界

- plan hash 防止无意漂移和陈旧计划，不防止受信宿主修改并重算 hash。
- portable runner 不是 OS sandbox；它用静态门禁、manifest allowlist、临时副本、
  最小环境与 timeout 降低风险。
- full Doctor 是可选增强，不是普通创建依赖。
- 没有真实任务试用时，结构分不得升级为 utility claim。
