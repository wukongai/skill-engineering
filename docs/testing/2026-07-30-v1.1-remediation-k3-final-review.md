# 1.1 Remediation — K3 终审回执

日期：2026-07-30

评审人：K3

评审对象：Codex 按 [`Remediation Spec`](../specs/2026-07-30-v1.1-review-remediation-spec.md) 与 [`Implementation Plan`](../plans/2026-07-30-v1.1-review-remediation-plan.md) 完成的修复；证据：[`RED → GREEN`](2026-07-30-v1.1-remediation-red-green.md)、[`Codex E2E`](2026-07-30-v1.1-codex-e2e.md)。

## Verdict

**approved** — CR-01 至 CR-06 全部修复并有 RED→GREEN 回归；K3 Spec Review 的 C1/C2、I1–I5 全部按回执方向收口；静态门禁、隔离 smoke 与本终审独立复跑全部通过。仅 2 个 P2 非阻断项，不阻碍进入 Hermes E2E 阶段。

## Findings

### P0

无。

### P1

无。

### P2

1. **`creation_review.py` 的 `GATE_DEDUCTIONS` 缺少 CG008 映射**(`skills/skill-engineering/scripts/creation_review.py:30-41`)。CG008（复杂工作流缺失败处理）仍由 `content_gate.py:375-380` 产出，评审时落入默认扣分 `{"engineering": 10}` 而非显式维度映射。不影响阻断语义（CG008 仍使 status=blocked)，只影响分数维度的可解释性。最小修订：为 CG008 补一行显式映射（建议 `{"stability": 20, "engineering": 10}`)。
2. **`native_plan.py` plan 中 `"hash_version": 1`**(`native_plan.py:245`)。字段名与 BuildPlan 的 `hash_version=2` 语义不同（前者是 native plan 自身 schema 的 hash 代际），同名词并存可能误导读者。最小修订：改名 `plan_hash_version` 或在字段旁注释其独立于 BuildPlan hash_version。纯命名问题，无行为影响。

## CR-01 至 CR-06 逐项结论

| 编号 | 结论 | 终审证据 |
|---|---|---|
| CR-01 主路由跳 legacy scaffold | **fixed** | `SKILL.md:24` 已改为 Native Authoring + `native_plan.py preview/apply/verify` + `skill_self_test.py`;legacy `create` 仅 1.0/CI `scaffold_only`(`scaffold.py:292`)。 |
| CR-02 Brief 原文落盘 | **fixed** | `journey.py:399-404` `save_authoring_brief()` 在持久化边界自动 `sanitize_brief()`，只保存脱敏副本且不改调用对象；`native_plan.py:116-119` apply 前对 Brief 再做敏感值扫描。 |
| CR-03 单安装无法评分 | **fixed** | `creation_review.py` 仅标准库，`python -I -S` 下本终审实测输出 100/A、四维、`utility_claim=False`;`creation-review.md` 已改 portable 主路径、完整 Doctor 为可选增强。 |
| CR-04 自测只编译 | **fixed** | `skill_self_test.py` manifest 驱动真实执行（临时副本、`-B`、最小 env、timeout、exit/stdout/stderr/JSON key 断言），并校验 `target_unchanged`;Codex E2E 记录了真实失败→修复→复跑。 |
| CR-05 Content Gate 漏检 | **fixed** | symlink、`.env.*`/`*.env`、PEM/证书容器、resolve 越界、frontmatter name≠目录名、中英文副作用、CG010 manifest 校验均已实现并有回归（content gate 27 项测试）。 |
| CR-06 `content_status` 可篡改 | **fixed** | `hash_version=2` 覆盖 `content_status`(`scaffold.py:36-39,292-293`);legacy 计划按 v1 hash 兼容且保守映射 `scaffold_only`;tamper 回归测试在 211 项中通过。 |

## K3 Spec Review 修订项逐项结论

| 编号 | 结论 | 说明 |
|---|---|---|
| C1 portable parity 架构 | **按回执收口** | portable profile 与 Doctor v2 共用四维/权重（20/25/25/30)/等级阈值；`assessment.coverage` 与 `not_covered` 显式声明；无全称 parity 宣称，parity 由枚举 fixture 测试守护。残留 CG008 映射见 P2-1。 |
| C2 plan 威胁模型与 hash 排除 | **按回执收口** | `MUTABLE_EXECUTION_FIELDS`(applied/verified/时间戳）显式排除；RED→GREEN 文档明确“防漂移不防篡改，宿主为受信执行者”。 |
| I1 R8 “必须不同”收窄 | **按回执收口** | fixture 允许 Codex/Kimi 共享事实正确的路径与工具，仅 Hermes 保持 `skill_manage` 差异；五宿主共享同一真实 candidate fingerprint(`2113b4e0…`,由 `fingerprint_tree` 实测比较）。 |
| I2 runner 残留风险声明 | **按回执收口** | 输出含 `runner_boundary.sandbox=false` 与控制清单；执行顺序为静态门禁先于真实执行；非 Python 报 `uncovered` 不静默跳过。 |
| I3 行为样例形式 | **按回执收口** | PCR202 接受 `SKILL.md` 内示例，不强制 tests 目录；仅声明脚本时强制 manifest。 |
| I4 `creation-review.md` 主路径 | **按回执收口** | 文档已改 portable 主路径 + 完整 Doctor 可选增强；`test_doctor_wrapper` 的退出 2 期望仅保留给完整 Doctor 场景。 |
| I5 brief-id 引用 | **按回执收口** | `native_plan.py` 只接受 `--brief-id` 并从 `.skill-engineering/authoring-briefs/` 加载，拒绝游离 JSON;id 校验 + schema 校验 + 敏感值重扫。 |

## 本终审实际复跑

```text
pytest -q                                   211 passed
ruff check .                                All checks passed
skill-engineering lint                      ok
production Doctor                           0 FAIL/0 WARN, 100/A(仅 structural-readiness)
credential-lint --all                       通过
git diff --check                            通过
check-release --version 1.1.0               passed(1.1 保持 Unreleased)
git log                                     HEAD 仍为 13045ba,无未授权 commit
```

独立隔离 smoke（复制 `skills/skill-engineering` 到 `/tmp/k3-final-iso`，全部 `python -I -S`，不导入仓库 src 与 site-packages):

```text
自举 content_gate.py      pass
自举 creation_review.py   100/A, utility_claim=False
自举 skill_self_test.py   pass, target_unchanged=true
native_plan preview       preview_ready(4 文件, review 100/A),未写 target
native_plan apply         applied(mkdtemp 临时副本 → 复验 → 原子改名)
native_plan verify        verified
写后 self_test            pass
漂移负例                  拒绝(target 与 plan 不一致/重复 apply 阻断)
```

## 是否允许进入下一阶段

**允许进入 Hermes E2E 阶段。** Hermes 无 Creator 真实 E2E 仍是发布前唯一硬门禁；通过后再按用户授权依次执行 commit → push → tag → GitHub Release。P2 两项可在 Hermes E2E 期间顺带修复，不需要独立返工轮次。

边界确认：本终审为只读交叉评审；未修改任何实现、Spec、Plan 或测试；未执行 commit/push/tag/release;Hermes E2E 未由任何 fixture 或本文替代。
