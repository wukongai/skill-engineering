# Skill Engineering 1.0 四个 Use Case 发布验证

状态：本地 Release Candidate 门禁通过，等待远程默认分支安装验证

总体结论：passed（本地候选）

## 验证目标

本轮不是重新制作展示截图，而是把文章里的四类真实用户行为绑定到同一个 `1.0.0` 发布候选，重新执行 Preview、Apply、失败恢复、结构检查和发布门禁。历史截图用于展示真实对话；本轮自动回归用于证明相同候选仍守住这些行为。

## 候选版本

- 产品版本：`1.0.0`；
- 候选 commit：测试完成并提交后填写；
- 唯一 Agent Skill：`skills/skill-engineering/`；
- 标准远程命令：`npx skills add wukongai/skill-engineering --skill skill-engineering -g -a codex -y`。

## 四个案例

| Use Case | 候选回归 | 用户行为与边界 | 既有真实执行证据 |
|---|---|---|---|
| 会议纪要行动项 | passed | atomic；写前预览；只提取有原文依据的决策、负责人和日期；普通摘要不触发 | `2026-07-16-real-user-simulation-report.md` 与用户旅程截图 |
| GitHub Issue 实施计划 | passed | composite；只读分析用户提供的 Issue 数据；修改标签、评论或关闭 Issue 必须停止 | 已验收的独立 Codex 用户任务与文章完整对话截图 |
| 本地 Web 应用验收 | passed | adapter；本地 fixture；浏览器操作有批准边界；保存截图、CSV、JSON 和 Markdown 证据 | 已验收的独立 Codex 用户任务与文章完整对话截图 |
| 多文件研究证据包 | passed | composite；契约和三类回归分层；高风险外部发布计划自动回滚；收窄后重新规划 | 已验收的独立 Codex 用户任务、执行日志和文章完整对话截图 |

自动化入口：

```bash
python3 -m pytest -q tests/test_v1_release.py
```

这组测试对每个案例真实生成不可变预览计划，确认预览阶段没有目标文件，再应用同一计划并执行 lint/team Doctor。研究证据包还单独验证第一次高风险计划失败后目标目录被完整清理。

## 结构健康与真实效用边界

候选回归证明的是：创建和恢复流程、文件结构、触发边界、审批边界以及确定性检查没有退化。历史独立任务证明四类 Skill 曾在真实用户会话中完成创建和验收。它不声称已经跨所有模型、所有真实 GitHub 仓库或所有生产网站验证通用效用。

## 完整门禁

| 门禁 | 结果 |
|---|---|
| 四个 Use Case 回归 | passed：6 项，含四案创建与研究包失败回滚 |
| 全量 pytest | passed：128 tests |
| Ruff | passed |
| Agent Skill validation | passed |
| production Doctor | passed：0 FAIL / 0 WARN，100/A（仅结构准备度） |
| credential lint | passed |
| diff check | passed |
| wheel build | passed：`skill_engineering-1.0.0.tar.gz`、`skill_engineering-1.0.0-py3-none-any.whl` |
| 干净虚拟环境安装与 CLI smoke | passed：预览未写入、同一 plan Apply、team Doctor 0 FAIL / 0 WARN |
| 远程默认分支标准安装 | 等候候选首次 push 后执行 |

只有所有本地门禁通过后才允许推送候选；只有远程默认分支安装通过后，才可以请求 `v1.0.0` tag 和 GitHub Release 授权。
