# Skill Engineering Apache-2.0、署名与品牌边界实施计划

状态：Completed；全量门禁、PM 独立复核与 OB task 已完成；引用 Spec：`docs/specs/2026-07-18-apache-2.0-attribution-spec.md`

## 阶段

1. **Inventory**：盘点根许可证、包元数据、README、PRODUCT、CONTRIBUTING、版权安装指南、版本/发布材料、测试与历史决策中的 MIT 声明。
2. **Decision record**：新增 ADR 0006，记录 Apache-2.0、`NOTICE`、推荐 citation、品牌边界和不采用自定义署名许可证的原因；将 ADR 0005 标记为 superseded。
3. **License surface**：替换根 `LICENSE`，新增 `NOTICE`、`CITATION.cff`、`TRADEMARKS.md`，更新 `pyproject.toml` SPDX/metadata。
4. **Documentation alignment**：统一 README、PRODUCT、CONTRIBUTING、版权安装指南、VERSIONING、CHANGELOG、TASK、Sprint、release log、docs index 与日记。
5. **Regression evidence**：新增许可证政策测试，运行 pytest、Ruff、官方 Skill validation、production Doctor、credential lint、文档链接检查、构建元数据核验与 `git diff --check`。
6. **Independent review**：交给 PM 做只读验收复核，修复其确认的阻断问题并重跑受影响验证。
7. **Task closure**：更新仓库 Journey 与 OB task，记录完成事实、交付物和剩余独立审批点。

## 复杂度与影响

- 代码运行时行为：无变化。
- 法律/分发元数据：MIT 迁移为 Apache-2.0，新增通知、引用与品牌边界。
- 用户可见变化：商业使用仍允许；再分发与修改通知义务更明确；增加显式专利授权。
- 兼容性：v1.0 尚未 tag，因此在首个稳定版前完成迁移；历史 MIT 文件作为决策历史保留并标注已被取代。

## 恢复

本计划只修改版本化文件和任务记录，不执行 tag、release、push 或全局安装。若验证失败，保留失败证据并继续修正，不以回退许可证来掩盖不一致。

## 完成定义

Spec、Plan、ADR、许可证资产、用户文档、项目治理、测试与 release evidence 对齐；PM 复核无阻断项；OB task 和 Journey 关闭。本计划完成时，commit、push、tag、GitHub Release、PyPI 发布与 Global/Profile 安装仍需各自明确批准；后续 commit、push、`v1.0.0` tag 和 GitHub Release 已分别获批并完成，PyPI 与 Global/Profile 仍未执行。
