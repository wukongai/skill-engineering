# 2026-07-20

- 用户要求调研记忆中名为 “skilled spectrum” 的微软官方 Skill 工程竞品；核实实际项目为 `microsoft/waza`，`microsoft/skills` 是其目标生态与内容库之一。
- 固定上游 commit `cf05e487ed967d497a138738514d24457bc45f2a`、tag `v0.38.3`、MIT 许可证和 2026-07-20 快照。
- 只读审计 Waza 的 PRD、README、Changelog、runner、baseline、grader、gate、snapshot、redaction、adversarial、dashboard、MCP/JSON-RPC 和测试结构；未安装 release、未调用模型、未运行真实效用对照。
- 结论：Waza 是 Skill CI、自动行为测试、A/B benchmark、grader 和结果分析平台；Skill Engineering 是创建、维护、holdout、演进和安全发布治理系统。
- 核对 `src/skill_engineering/evaluation.py`：本项目原有 A/B 能力真实存在，已比较 baseline/candidate、development/holdout、high-risk 和 negative transfer，并绑定输入 hash；但它只消费外部 rollout 结果，不自动运行 Skill、模型、工具、trial 或 grader。
- 用户明确选择不搬运 Waza 代码和风格，而是完整学习公开能力结构，在本项目下 clean-room 重写等价功能。
- 新增 Waza 调研报告，记录调研方案、固定证据、能力对比、采用/拒绝边界和 limitations。
- 将零散的 provider-neutral runner、pressure 和 multi-turn eval 合并为 `Behavior Evaluation Lab` Backlog Epic，拆分为内部分析、证据模型、runner、grader、A/B 统计、CI/replay、adversarial/E2E 七个阶段。
- 当前 Architecture Guardian Phase 1 不扩范围；新窗口先完成内部分析和 ADR/Spec/Plan，明确升级为 Task/Sprint 后才允许修改 `src/`。
- 创建新任务 handoff；本窗口作为竞品研究任务收口。未 commit、未 push、未创建 tag 或 release。

## skill2loop Observation Boundary 调研

- 固定并审计 `1va7/skill2loop` commit `1b21514ce383e4415d9d988c82a840042cb20b13`，确认其当前边界是 session/hook ingest、Episode/Trace/Feedback、启发式聚类与指标、eval seed 草案和人工 review，不包含完整候选、holdout、发布和回滚后半环。
- 上游公开测试 16 passed，但出现 SQLite connection `ResourceWarning`；本地 `tests/test_evolution.py` 与 `tests/test_blueprint.py` 定向测试 16 passed。
- 对照确认 Skill Engineering 已拥有 `SkillRun -> Proposal -> Candidate -> Evaluation -> Version -> Release` 后半环，主要缺口是由真实 session 自动形成脱敏、低噪声、可审计 SkillRun 的前置观测链路。
- 决定不复制或 vendor 上游源码；把上游公开行为和架构思想解析为本地 Spec，再按现有代码风格 clean-room reimplementation。
- 固定目标边界为 `Session/Hook -> Observation -> Promotion Gate -> existing SkillRun`；Observation 不拥有正式 Proposal、Eval、Candidate、Version 或 Release。
- 完成研究报告、采用/冲突矩阵、Backlog 和新任务 Handoff；本窗口作为调研任务收口，不改产品代码，不扩张当前 v2 Phase 1。
- 文档收口门禁中 Ruff、官方 Skill validation、production Doctor 100/A、credential lint 和 diff check 通过；全量 pytest 为 132 passed、1 failed。唯一失败是既有 installed-wrapper 缺 runtime 模拟测试，当前 `.venv` editable install 在 `python -I` 下仍能导入本地包；相关测试和 wrapper 无本窗口 diff，本调研任务未越界修复。
- 下一任务建议先完成 Observation Boundary Spec、跨版本 ADR 和实施 Plan 预览；用户确认同一计划后，再在独立 candidate 中重写并验证。
